releases:
  - name: builds-api
    chart: ./charts/builds-api/
    namespace: builds-api
    createNamespace: true
    values:
      - ./values/{{ .Environment.Name }}.yaml*

environments:
  tools: {}
  toolsbeta: {}
  local: {}
